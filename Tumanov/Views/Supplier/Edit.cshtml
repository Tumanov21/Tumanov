@model Site.Models.Data.Supplier

<form asp-action="Edit" asp-controller="Supplier" asp-route-id="@Model.Id">
    <div>
        <label asp-for="Email">Email</label>
        <input type="text" asp-for="Email" />
    </div>

    <div>
        <input type="hidden" asp-for="Company.Name" />
    </div>

    <div class="form-group">
        <input type="submit" value="Сохранить" class="btn btn-outline-primary" />
    </div>
</form>